--- STRIPE
// stripe login
stripe login

// forward to localhost for testing stripe webhook
stripe listen --forward-to localhost:9000/stripe-webhooks

export GOOGLE_APPLICATION_CREDENTIALS="....json"
